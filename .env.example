# ===============================================
#         CONFIGURAÇÃO MAILMIND AI
# ===============================================

# -----------------------------------------------
# API do Google Gemini
# -----------------------------------------------
# Obrigatório: Sua chave de API do Google Gemini.
# Obtenha em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="sua_chave_gemini_aqui"

# Modelo a ser utilizado. Recomenda-se "gemini-2.5-flash" para balancear custo e performance.
GEMINI_MODEL="gemini-2.5-flash"

# -----------------------------------------------
# Configuração da Aplicação
# -----------------------------------------------
# Porta em que a aplicação irá rodar localmente (via docker-compose).
PORT=8080

# Ambiente de execução. Use "development" ou "production".
ENVIRONMENT="development"

# Chave de API para proteger endpoints (opcional, mas recomendado para produção).
# Se definida, deve ser enviada no header "X-API-Key".
API_KEY="sua-chave-secreta-aqui"

# -----------------------------------------------
# Configuração de Cache (Redis)
# -----------------------------------------------
# URL de conexão com o Redis. O docker-compose já configura isso automaticamente.
REDIS_URL="redis://redis:6379/0"

# Tipo de cache. Use "RedisCache" para produção ou "SimpleCache" para desenvolvimento sem Redis.
CACHE_TYPE="RedisCache"

# Tempo padrão de expiração do cache em segundos (86400s = 24 horas).
CACHE_DEFAULT_TIMEOUT=86400

# -----------------------------------------------
# Configuração de Rate Limiting
# -----------------------------------------------
# Habilita ou desabilita o rate limiting. Use "true" ou "false".
RATE_LIMIT_ENABLED="true"

# Limite padrão de requisições. Formato: "<numero> per <unidade>" (minute, hour, day).
RATE_LIMIT_DEFAULT="100 per hour"

# -----------------------------------------------
# Configuração de Email (Opcional)
# -----------------------------------------------
# Habilita o envio de emails (ex: para notificar curadores). Use "true" ou "false".
SMTP_ENABLED="false"

# Host do seu servidor SMTP.
SMTP_HOST="smtp.sendgrid.net"

# Porta do servidor SMTP.
SMTP_PORT=587

# Usuário para autenticação no SMTP.
SMTP_USER="apikey"

# Senha para autenticação no SMTP (para SendGrid, é a chave de API).
SMTP_PASSWORD="sua_chave_sendgrid_aqui"

# Endereço de email para o remetente (ex: nao-responda@suaempresa.com).
NOREPLY_ADDRESS="nao-responda@suaempresa.com"

# Endereço de email do curador que receberá as notificações.
CURATOR_ADDRESS="curador@suaempresa.com"

# -----------------------------------------------
# Monitoramento de Erros (Sentry)
# -----------------------------------------------
# DSN do Sentry para monitoramento de erros em produção (opcional).
SENTRY_DSN=""

